# from tkinter import *
# import menu_controller
#
# root = Tk()
#
# btn_refresh = Button(root, text='Refresh',  width=7, height=1, fg='red', font='arial 10')
# btn_refresh.grid(row=0, column=0)
# # btn_refresh['command'] = menu_controller.refresh(root)
#
# btn_settings = Button(root, text='Settings',  width=7, height=1, fg='red', font='arial 10')
# btn_settings.grid(row=0, column=1)
#
# btn_quit = Button(root, text='Exit',  width=7, height=1, fg='red', font='arial 10')
# btn_quit.grid(row=0, column=2)
#
# pair_list = Listbox(root, height=5, width=15, selectmode=SINGLE)
# list_str = ['BTC/USD', 'ETH/USD', 'ZEC/USD', 'ZEC/BTC']
# for i in list_str:
#     pair_list.insert(END, i)
# pair_list.grid(row=1, column=0)
#
# root.mainloop()
